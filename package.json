{
  "name": "npm-version-bumper",
  "private": "true",
  "version": "0.1.0",
  "devDependencies": {
    "mversion": "*"
  },
  "scripts": {
    "pkgversion": "echo $npm_package_version",
    "version": "mversion",
    "version-release": "mversion patch -m 'Release %s' --tag '%s'",
    "version-devel": "mversion preminor && git commit -am \"Preparing for next development iteration ($(npm run pkgversion))\"",
    "bump": "npm run version-release && npm run version-devel",
    "broadcast": "git remote | while read remote; do git push $remote master && git push --tags; done"
  }
}
